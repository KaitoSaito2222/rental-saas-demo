{
  "name": "property-copilot-demo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @property-copilot/api & npm run dev --workspace @property-copilot/web & wait",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:migrate:dev": "npm run db:migrate:dev --workspace @property-copilot/api",
    "db:seed": "npm run db:seed --workspace @property-copilot/api"
  }
}
