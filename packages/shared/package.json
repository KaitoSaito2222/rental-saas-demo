{
  "name": "@property-copilot/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'shared package has no standalone tests yet'",
    "build": "echo 'shared package is source-only for the demo scaffold'"
  }
}
